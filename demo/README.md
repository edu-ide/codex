# Ilhae Chat UI Demo

Static chat UI prototype for local Ilhae/Codex agent workflows.

Open `index.html` directly in a browser, or serve the folder:

```bash
python3 -m http.server 4173 --directory demo
```
